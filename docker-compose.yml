services:
  app:
    build: .
    networks:
      my_network:
        ipv4_address: 172.30.0.5
    volumes:
      - ./server/public/uploads:/app/server/public/uploads
    environment:
      - VITE_PORT=80
    restart: unless-stopped

networks:
  my_network:
    external: true

